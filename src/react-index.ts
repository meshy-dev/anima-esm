// anima-esm — React entry: the vanilla core PLUS the optional <Figure> wrapper.
// This bundle (anima-react.min.mjs) imports react; the core bundle does not.

export * from "./index";
export { Figure } from "./react";
